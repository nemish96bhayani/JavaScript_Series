"use strict"; //treat all JS code as newer version

// alert(3 + 3); we are  using nodejs, not browser

console.log(3 + 3); //code readability should be hiigh

console.log("Nemish");

let name = "nemish";
let age = 21;
let isLoggedIn = true;
let state;

// number => 2 to power 53
// bigint
// string => ""
// boolean => true/false
// null => standalone value
// undefined => value wasn't asssign at right now
// symbol => unique

// object

console.log(typeof "nemish");

console.log(typeof undefined); // undefined data type
console.log(typeof null); // object datatype

// imp nul datatype is object a object but undefined is a data type
